programa
{
	
	funcao inicio()
	{
		real nota1, nota2, media

		escreva("Qual sua nota 1?: ")
		leia(nota1)

		escreva("Qual sua nota 2?: ")
		leia(nota2)

		media = (nota1 + nota2) / 2

		se (media >= 7) {
			escreva("Aprovado!")
		}

		senao {
			escreva("Reprovado!")
		}
		
	}
}
